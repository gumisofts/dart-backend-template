include: package:lints/recommended.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.gisila.g.dart"

linter:
  rules:
    - prefer_single_quotes
    - always_declare_return_types
    - avoid_print
